version: '3.9'

services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      - PORT=3001
      - JWT_SECRET=${JWT_SECRET:-change_this_secret_in_production}
      - UPLOADS_DIR=/uploads
      - DB_DIR=/data
      - FRONTEND_URL=http://photoshare.crookshannks.daecloud.co.za:14586
    volumes:
      - uploads:/uploads
      - db_data:/data
    networks:
      - app

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "${PORT:-14586}:80"
    depends_on:
      - backend
    networks:
      - app

volumes:
  uploads:
  db_data:

networks:
  app:
    driver: bridge
